# Example liquibase.properties (optional). If you COPY this to /liquibase/liquibase.properties
# or mount it at runtime (-v ./liquibase.properties:/liquibase/liquibase.properties),
# you can run just 'liquibase update' without passing URL/credentials via env.
changeLogFile=/liquibase/changelog/changelog-master.xml
